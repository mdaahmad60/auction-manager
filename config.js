// Source-only local mode preserves the standalone app and browser regression fixtures.
// Production builds replace this file and REQUIRE Supabase configuration.
window.APP_CONFIG = { localMode: true };
